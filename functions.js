/**
 * CHANGELOG
 *
 * Instruções para Revisores:
 * Este bloco de comentários registra as modificações significativas do arquivo.
 * Cada nova modificação deve ser adicionada no topo da lista.
 * Use o formato "Versão [número]: [Descrição da modificação]".
 * Mantenha a lista limitada às 4 últimas alterações para clareza e concisão.
 *
 * Versão 1.1: Refatoração para separar funções de manipulação do canvas.
 * Versão 1.0: Funções de manipulação de imagem e texto no canvas.
 */

// Estas funções foram integradas diretamente na classe BadgeGenerator no script.js
// para manter a coesão e o estado da aplicação em um único objeto.
// A separação neste arquivo não é necessária para a estrutura atual.
// No entanto, se o projeto crescer, estas funções podem ser extraídas e exportadas.

// Exemplo de como as funções poderiam ser estruturadas se estivessem em functions.js:
// export function drawBadge(generatorInstance) { ... }
// export function drawUserImageBehind(generatorInstance) { ... }
// export function drawTexts(generatorInstance) { ... }
// export function applySharpenFilter(imageData) { ... }
// E assim por diante.
